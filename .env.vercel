# Copy and paste these into the Vercel Dashboard -> Settings -> Environment Variables

# Replace this with the URL you get after deploying the backend to Render
VITE_API_URL=https://your-backend-url.onrender.com

# If your frontend uses the Groq key directly:
VITE_GROQ_API_KEY=your_actual_groq_api_key_here
